---
import Icon from "node_modules/astro-icon/components/Icon.astro";
import { useTranslations, type Language } from "../../utils/i18n";
import { url } from "../../utils/url";

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section
  id="hero"
  class="relative min-h-screen flex items-center justify-center pt-20 pb-16 overflow-hidden"
>
  <div class="container-custom">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="space-y-8 animate-fade-up">
        <div class="space-y-4">
          <p class="text-primary-600 dark:text-primary-400 font-medium text-lg">
            {t("hero.greeting")}
          </p>
          <h1
            class="text-5xl md:text-6xl lg:text-7xl font-bold text-light-text dark:text-dark-text"
          >
            <span class="block">{t("personal.name")}</span>
            <span class="block text-gradient-primary"
              >{t("personal.surname")}</span
            >
          </h1>
          <h2
            class="text-2xl md:text-3xl font-semibold text-light-text-muted dark:text-dark-text-muted"
          >
            {t("personal.specialty")}
          </h2>
        </div>

        <p
          class="text-lg text-light-text-muted dark:text-dark-text-muted max-w-2xl leading-relaxed"
        >
          {t("hero.description")}
        </p>

        <div class="flex flex-wrap gap-3">
          {
            [
              "AI Research",
              "Data Science",
              "ML Optimization",
              "LLM",
              "Deep Learning",
            ].map((skill) => <span class="badge">{skill}</span>)
          }
        </div>

        <div class="flex flex-wrap gap-4">
          <a href="#projects" class="btn btn-primary">
            <span>{t("hero.cta.primary")}</span>
            <Icon class="w-5 h-5 ml-2" name="mdi:arrow-right" />
          </a>
          <a href="#contact" class="btn btn-secondary">
            <Icon class="w-5 h-5 mr-2" name="mdi:email-outline" />
            <span>{t("hero.cta.secondary")}</span>
          </a>
        </div>
      </div>

      <div class="relative lg:block animate-fade-up animation-delay-200">
        <div class="relative z-10">
          <div class="relative w-full max-w-md mx-auto aspect-square">
            <div
              class="absolute inset-0 bg-gradient-to-br from-primary-500 to-accent-500 rounded-3xl blur-2xl opacity-20"
            >
            </div>
            <div class="relative card card-hover overflow-hidden">
              <img
                src={url("/images/profile/photo.jpg")}
                alt={t("personal.fullname")}
                class="w-full h-full object-cover"
                loading="eager"
              />
            </div>
          </div>

          <div
            class="absolute -top-4 -right-4 card p-4 animate-float animation-delay-100"
          >
            <div class="flex items-center gap-3">
              <Icon
                class="w-8 h-8 text-primary-600"
                name="mdi:school-outline"
              />

              <div>
                <div class="text-2xl font-bold text-primary-600">BSc</div>
                <div
                  class="text-xs text-light-text-muted dark:text-dark-text-muted"
                >
                  AI & DS
                </div>
              </div>
            </div>
          </div>

          <div
            class="absolute -bottom-4 -left-4 card p-4 animate-float animation-delay-300"
          >
            <div class="flex items-center gap-3">
              <Icon class="w-8 h-8 text-accent-600" name="mdi:check-decagram" />

              <div>
                <div class="text-2xl font-bold text-accent-600">GPA</div>
                <div
                  class="text-xs text-light-text-muted dark:text-dark-text-muted"
                >
                  4.95/5.0
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .animation-delay-100 {
    animation-delay: 0.1s;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-300 {
    animation-delay: 0.3s;
  }
</style>
